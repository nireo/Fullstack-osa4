const Blog = require('../models/blog')

// just a to test jest
const dummy = (blogs) => {
    return 1;
}

// count up all the likes
const totalLikes = (blogs) => {
    
}

module.exports = {
    dummy,
    totalLikes
}